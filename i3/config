# Mod key
set $mod Mod4

# Application variables
set $browser  firefox
set $explorer urxvt -e ranger
set $network  nm
set $music    urxvt -e ncmpcpp
set $torrent  urxvt -e rtorrent
set $terminal urxvt
set $viewer   zathura

# Configuration for gaps
# gaps inner 20
# gaps outer -10
# smart_gaps on

# Font for window titles
font pango: TamzenForPowerline 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Application shortcuts
bindsym $mod+Return           exec --no-startup-id $terminal
bindsym $mod+b                exec $browser
bindsym $mod+m                exec --no-startup-id $music
bindsym $mod+n                exec $network
bindsym $mod+t                exec --no-startup-id $torrent
bindsym $mod+y                exec --no-startup-id $explorer
bindsym $mod+u                exec $viewer
bindsym Print                 exec --no-startup-id rm ~/.tmp/tmp.png -f && scrot ~/.tmp/tmp.png && xclip ~/.tmp/tmp.png -selection clipboard -t image/png && notify-send -u "low" "Took screenshot (window)"
bindsym --release Shift+Print exec --no-startup-id rm ~/.tmp/tmp.png -f && scrot -s ~/.tmp/tmp.png && xclip ~/.tmp/tmp.png -selection clipboard -t image/png && notify-send -u "low" "Took screenshot (area)"

# Kill focused window
bindsym $mod+Shift+q kill

# Start rofi
bindsym $mod+d exec rofi -show run

# Change focus: vim bindings
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Change focus: cursor keys
bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

# Move focused window: vim bindings
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Move focused window: cursor keys
bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

# Split in horizontal orientation
bindsym $mod+Shift+v split h

# Split in vertical orientation
bindsym $mod+v split v

# Enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Focus the parent container
bindsym $mod+a focus parent

# Focus the child container
bindsym $mod+Shift+a focus child

# Killing applications
bindsym $mod+Shift+d focus parent,kill

# Reload the configuration file
bindsym $mod+Ctrl+r reload

# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Workspace configuration
set $ws1  "1:1"
set $ws2  "2:2"
set $ws3  "3:3"
set $ws4  "4:4"
set $ws5  "5:5"
set $ws6  "6:6"
set $ws7  "7:7"
set $ws8  "8:8"
set $ws9  "9:9"
set $ws10 "10:0"

# Default workspaces for applications
assign [class="Firefox"]  $ws1
assign [class="mpv"]      $ws9

# Handle floating dialogs
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable

# Navigating to workspaces
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Shortcuts for moving workspaces
bindsym $mod+Ctrl+h     move workspace to output right
bindsym $mod+Ctrl+l     move workspace to output left
bindsym $mod+Ctrl+Right move workspace to output right
bindsym $mod+Ctrl+Left  move workspace to output left

# Shortcut for i3lock
bindsym $mod+Shift+x exec --no-startup-id lock

# Resize window (can also use mouse)
mode "resize" {
  # Vim bindings
  bindsym h resize shrink width 5 px or 5 ppt
  bindsym j resize grow height 5 px or 5 ppt
  bindsym k resize shrink height 5 px or 5 ppt
  bindsym l resize grow width 5 px or 5 ppt

  # Cursor key bindings
  bindsym Left resize shrink width 5 px or 5 ppt
  bindsym Down resize grow height 5 px or 5 ppt
  bindsym Up resize shrink height 5 px or 5 ppt
  bindsym Right resize grow width 5 px or 5 ppt

  # Back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer -D pulse set Master 1%+"
bindsym XF86AudioLowerVolume exec --no-startup-id "amixer -D pulse set Master 1%-"
bindsym XF86AudioMute        exec --no-startup-id "amixer -D pulse set Master 0+ toggle "

# Screen brightness controls
bindsym XF86MonBrightnessUp   exec --no-startup-id "sudo adjust-brightness +50"
bindsym XF86MonBrightnessDown exec --no-startup-id "sudo adjust-brightness -50"

# Touchpad controls
bindsym XF86TouchpadToggle exec toggle-touchpad

# Media player controls
bindsym XF86AudioPlay  exec --no-startup-id mpc play
bindsym XF86AudioPause exec --no-startup-id mpc pause
bindsym XF86AudioNext  exec --no-startup-id mpc next
bindsym XF86AudioPrev  exec --no-startup-id mpc prev
bindsym XF86AudioStop  exec --no-startup-id mpc pause

# MPD controls
bindsym $mod+p            exec --no-startup-id mpc toggle
bindsym $mod+bracketleft  exec --no-startup-id mpc prev
bindsym $mod+bracketright exec --no-startup-id mpc next

# Color definitions
set $base03    #002b36
set $base02    #073642
set $base01    #586e75
set $base00    #657b83
set $base0     #839496
set $base1     #93a1a1
set $base2     #eee8d5
set $base3     #fdf6e3
set $yellow    #b58900
set $orange    #cb4b16
set $red       #dc322f
set $magenta   #d33682
set $violet    #6c71c4
set $blue      #268bd2
set $cyan      #2aa198
set $green     #859900

set $active-bar-bg-color     $base3
set $inactive-bar-bg-color   $base1
set $active-bar-text-color   $base03
set $inactive-bar-text-color $base03

# Window colors
#                       border     background    text      indicator
client.focused          $base03    $yellow       $base2    $yellow
client.unfocused        $base03    $base03       $base2    $base03
client.focused_inactive $base03    $base03       $base2    $base03
client.urgent           $base03    $base03       $red      $base03

hide_edge_borders smart
for_window [class="^.*"] border pixel 5
new_float none

# Configure fonts
exec --no-startup-id xset +fp ~/.fonts
exec --no-startup-id xset fp rehash

## Configure startup applications
# Appearance and notifications
exec --no-startup-id /usr/lib/gnome-settings-daemon/gsd-xsettings
exec --no-startup-id dunst -c ~/.config/dunst/dunstrc
exec --no-startup-id compton -f

# Tray applets
exec --no-startup-id redshift-gtk
exec --no-startup-id blueman-applet
exec --no-startup-id nm-applet

# Music player
exec --no-startup-id i3-msg "exec --no-startup-id ps -e | grep --quiet mpd || mpd"

# Configure displays
exec_always --no-startup-id setup-displays
