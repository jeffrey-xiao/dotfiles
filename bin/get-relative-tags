#!/bin/sh

for tags_file in "$@"; do
  tags_path_length=$(basename "$tags_file" | wc --chars)
  awk --field-separator="\\t" "{OFS=\"\\t\"; \$2 = substr(\$2, $tags_path_length - 3); print}" "$tags_file" | grep --invert-match --text ^!
done

